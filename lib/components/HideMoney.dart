import 'package:flutter/material.dart';

class HideMoney extends StatelessWidget {
  const HideMoney({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(width: 200, height: 30, color: Colors.grey[100]);
  }
}
